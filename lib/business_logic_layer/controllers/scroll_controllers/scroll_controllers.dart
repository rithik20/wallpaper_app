import 'package:flutter/material.dart';

class ScrollControllers {
  ScrollController searchImageScrollController = ScrollController();
  ScrollController curatedImageScrollController = ScrollController();
}
